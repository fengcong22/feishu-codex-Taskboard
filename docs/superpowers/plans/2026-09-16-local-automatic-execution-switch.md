# 本机自动剪辑开关与课程标题

用户已确认将环境变量总开关改为可点击、持久化的本机设置，并要求卡片优先显示命名字段内容。无需重新确认已授权设计。

目标：左侧本机设置对话框提供总开关，学科配置提供直达入口；立即生效且重启保留。默认关闭，既有环境变量只作为尚未保存设置时的初始值。学科模式、可信来源、包白名单、模拟来源和 loopback 限制继续生效。

持久化复用 taskboard_settings：automatic-execution，值 {enabled:boolean}，版本用于并发比较交换。GET/PUT /api/local/settings/automatic-execution 返回 {setting:{enabled,version}}，PUT 要求 {enabled,expectedVersion}、JSON、web 专用 header、loopback Host/Origin；不接受远端代理设置。保存成功广播 automatic-execution.updated。

关闭后取消尚未开始的 automatic 延迟/排队预约，任务回到 todo；保留运行中的任务和手动操作。所有启动前资格检查读取当前策略，异步等待后再次检查。开关开启本身不枚举并启动旧任务。界面失败不假报保存成功，多窗口冲突重新加载。

- [x] 数据库/API 测试先验证默认值、保存、冲突、格式与来源校验、重启持久化及动态 meta。
- [x] 协调器测试验证关闭取消延迟/排队、启动途中再次检查、保留运行/手动任务、模拟不自动；再实现。
- [x] UI 测试验证设置入口、状态加载、点击保存、失败/冲突、学科直达和多窗口同步；再实现。
- [x] 标题测试验证命名字段优先、空白回退、历史记录显示、自定义标题保留与原始来源不变；再实现。
- [x] 更新 AGENTS.md、README；执行隔离 HTTP 联调、组件及完整回归，独立评审并修复两项反馈，复查无剩余问题。完整回归的环境波动与单项复验结果见下。
- [x] 检查本机运行状态与数据摘要：服务已停止，活动运行/claim/执行预约/上传均为 0。本轮交付可评审源码，保留服务停止状态，不自动恢复真实监听、不重跑生产任务、不改变实际开关。加载新版时先冷备份，再按原本机配置启动。

目录分工：主代理负责 database 设置方法、app 设置接口/动态策略、协调器、Bridge 模拟保护和文档；UI 代理负责 web；标题代理负责独立标题帮助函数和 app/database 中标题映射。所有生产配置/凭据/状态留在 Git 忽略目录。

评审追加修复：手动接管自动延迟预约时持久化触发方式，确保关闭和重启仍保留手动请求；标题与通用 API 共用 240 字符上限，保留完整命名快照和有效 UTF-16 字符。两个问题分别补充真实 SQLite 重启、长名称历史显示及属性修改回归。

验证记录（2026-09-16）：

- `node --test --test-concurrency=2` 最终完整运行 1652 项：1647 通过，3 项平台跳过，2 项环境失败（随机分配 Node fetch 禁用端口；Wrangler 本地连接失败）。失败两项使用 `--test-concurrency=1 --test-name-pattern` 隔离复验，2/2 通过。最初另一条随机端口失败也已单项复验通过。不把完整运行描述为一次全绿。
- 最终协调器、真实 SQLite 触发方式恢复、阶段登记与课程标题回归 64/64 通过；设置 API 持久化/冲突/来源校验 4/4 通过；真实 HTTP 前后端契约已纳入组件测试。
- `npm --prefix taskboard run test:components`：84/84 通过。
- `npm --prefix taskboard run typecheck`、`npm --prefix taskboard run build:web`、`git diff --check` 通过。构建仅有现存 CSS 伪元素及 chunk 大小提示。
- 独立只读代码评审两项反馈修复后复核，无剩余可操作问题。
- 生产数据库 31 张表的行数/哈希，以及配置、凭据、Bridge 状态文件哈希前后一致；服务保持停止。本轮未合并、推送或部署。
